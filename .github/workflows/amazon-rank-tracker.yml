name: Amazon Rank Tracker

on:
  schedule:
    # GitHub schedule can be delayed or skipped, so try several off-peak minutes.
    # The script exits early if the sheet was updated in the last 55 minutes.
    - cron: "7,22,37,52 * * * *"
  workflow_dispatch:

concurrency:
  group: amazon-rank-tracker
  cancel-in-progress: false

jobs:
  run-rank-tracker:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env:
      TZ: Asia/Shanghai
      FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
      FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
      FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
      FEISHU_PRIVATE_OPEN_ID: ${{ secrets.FEISHU_PRIVATE_OPEN_ID }}
      SPREADSHEET_TOKEN: ${{ secrets.SPREADSHEET_TOKEN }}
      SPREADSHEET_URL: ${{ secrets.SPREADSHEET_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install browser
        run: |
          python -m pip install --upgrade pip
          pip install playwright
          python -m playwright install chromium --with-deps

      - name: Run tracker
        run: python -u rank_tracker_core.py --notify private --skip-if-recent 55
